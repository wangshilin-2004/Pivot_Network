This directory holds archived helper wrappers that are no longer part of the supported seller-client workflow.

If you need to run the current Windows client, use:

- `bootstrap/windows/install_and_check_seller_client.ps1`
- `bootstrap/windows/start_seller_client.ps1`
