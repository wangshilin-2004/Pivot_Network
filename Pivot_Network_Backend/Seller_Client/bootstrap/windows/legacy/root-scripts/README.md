This directory stores old root-level Windows scripts that were removed from the main seller-client surface during cleanup.

They are kept only for troubleshooting history and migration reference.

Use these supported entrypoints instead:

- `bootstrap/windows/install_and_check_seller_client.ps1`
- `bootstrap/windows/start_seller_client.ps1`
