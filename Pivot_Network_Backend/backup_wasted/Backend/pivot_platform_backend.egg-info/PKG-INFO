Metadata-Version: 2.4
Name: pivot-platform-backend
Version: 0.1.0
Summary: FastAPI + PostgreSQL backend scaffold for Pivot Network
Requires-Python: >=3.11
Description-Content-Type: text/markdown
Requires-Dist: alembic<2.0,>=1.14
Requires-Dist: email-validator<3.0,>=2.2
Requires-Dist: fastapi<1.0,>=0.115
Requires-Dist: httpx<1.0,>=0.28
Requires-Dist: psycopg[binary]<4.0,>=3.2
Requires-Dist: pydantic-settings<3.0,>=2.7
Requires-Dist: pwdlib[argon2]<1.0,>=0.2
Requires-Dist: sqlalchemy<3.0,>=2.0.36
Requires-Dist: uvicorn[standard]<1.0,>=0.34
Provides-Extra: dev
Requires-Dist: pytest<9.0,>=8.3; extra == "dev"

# Pivot Platform Backend

这是 `Backend` 目录下的基础后端开发环境，包含：

- `FastAPI` 应用骨架
- `PostgreSQL` 本地开发数据库
- `SQLAlchemy 2.x` 会话与模型
- `Alembic` 数据库迁移
- 一个最小 `users` 表与示例 API

## 目录结构

```text
Backend/
├── alembic/
├── backend_app/
│   ├── api/
│   ├── clients/
│   ├── core/
│   ├── db/
│   ├── repositories/
│   ├── schemas/
│   ├── services/
│   └── main.py
├── compose.yml
├── Dockerfile
├── pyproject.toml
└── tests/
```

## 快速启动

### 方式一：推荐，直接用 Docker Compose

```bash
cd /root/Pivot_network/Backend
docker compose up --build
```

这份 `compose.yml` 里，`backend` 容器默认使用 Linux 主机网络模式来访问本地映射出来的 PostgreSQL，主要是为了规避当前环境里的容器桥接网络异常。

启动后可访问：

- API 根路径：`http://localhost:8000/`
- Swagger 文档：`http://localhost:8000/docs`
- 健康检查：`http://localhost:8000/api/v1/health`
- 就绪检查：`http://localhost:8000/api/v1/ready`

### 方式二：本机 Python 3.11 开发

当前机器的默认 `python3` 是 `3.6.8`，不能直接用于这套后端。  
如果要本机运行，请显式使用 `python3.11`：

```bash
cd /root/Pivot_network/Backend
python3.11 -m venv .venv
source .venv/bin/activate
pip install -e .[dev]
docker compose up -d postgres
alembic upgrade head
uvicorn backend_app.main:app --reload
```

如果你选择本机运行 FastAPI，而数据库用 compose 启动，请确认 `.env` 中：

```env
BACKEND_POSTGRES_HOST=localhost
```

当前配置统一使用 `BACKEND_` 前缀，避免和系统已有的 `DEBUG` 一类环境变量冲突。

## 迁移命令

初始化数据库：

```bash
alembic upgrade head
```

创建新迁移：

```bash
alembic revision -m "describe_change"
```

## 当前示例接口

- `GET /api/v1/health`
- `GET /api/v1/ready`
- `GET /api/v1/users/`
- `POST /api/v1/users/`

示例创建用户：

```bash
curl -X POST http://localhost:8000/api/v1/users/ \
  -H "Content-Type: application/json" \
  -d '{
    "email": "buyer@example.com",
    "display_name": "Example Buyer",
    "role": "buyer",
    "password": "pivot12345"
  }'
```

## 下一步建议

这套骨架现在更偏“平台后端起步版”。后续可以继续往你文档里的方向扩：

- 鉴权与登录
- seller / buyer / admin 分角色 API
- AdapterClient 完整封装
- Swarm 同步 worker
- 更完整的业务表与索引
